memory_limit = 512M

; nginx is the per-route gatekeeper (1m globally, 8m on the torrent upload routes) and
; Laravel's validation rules give the per-field message. PHP deliberately sits above
; both: capping it at 1M here would also kill the .torrent exception.
upload_max_filesize = 8M
post_max_size = 16M
